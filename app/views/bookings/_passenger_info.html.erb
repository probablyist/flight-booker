<div class="box">
  <table class="table">
    <thead>
      <tr>
        <td>Name</td>
        <td>Email</td>
      </tr>
    </thead>
    <% @booking.passengers.each do |p| %>
      <tr>
        <td><%= p.name %></td>
        <td><%= p.email %></td>
      </tr>
    <% end %>
  </table>
</div>

